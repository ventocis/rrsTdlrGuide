name: Deploy stack on tag push

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  deploy:
    uses: ./.github/workflows/util-deploy.yml
    with:
      environment: qa
      version_tag: ${{ github.ref_name }}
